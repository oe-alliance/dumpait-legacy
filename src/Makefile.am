## Process this file with automake to produce Makefile.in
bin_PROGRAMS = dumpait
dumpait_SOURCES  = main.cpp
dumpait_SOURCES += demux.cpp
dumpait_SOURCES += scanner.cpp
dumpait_SOURCES += log.cpp

dumpait_LDADD = $(LIBLTDL) @BASE_LIBS@ 


